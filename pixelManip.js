/**
 * contains the pixel manipulation, coloring logic of paint tool 
 */

let imageData = ctx.getImageData(0 , 0, W, H).data


let floodFill = (x, y, color) =>{
    // base condition to return is that if the color behind is n
}


